ライブラリ使用手順
1.'https://github.com/pololu/vl6180x-arduino'からライブラリをDLする．
2.ディレクトリの名前を'VL6180X'に変更する．
3.'<arduino>/libraries/'にコピーする．(多分'/usr/share/arduino/libraries')

一応'include.sh'も作った．(エラー処理とかしてないので悪しからず)
